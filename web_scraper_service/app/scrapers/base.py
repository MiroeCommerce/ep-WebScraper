"""
Base scraper class and utilities for all scraper modules.

Defines the abstract interface and common functionality for
fetching, parsing, and error handling.

All specific scrapers should inherit from `BaseScraper` and
implement the `fetch` and `parse` methods.
"""
